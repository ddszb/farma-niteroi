import styled from 'styled-components/native';
import colors from '../../assets/colors';

export const NoDosesText = styled.Text`
	color: ${colors.white};
	font-size: 26px;
	bottom: 50%;
	text-align: center;
`;
